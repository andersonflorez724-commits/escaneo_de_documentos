# ===========================================================================
#  Backend FastAPI - dependencias para Serverless Functions (Vercel)
#
#  Las funciones serverless de Vercel tienen un limite de ~250 MB
#  descomprimidos. PyTorch + EasyOCR pesan ~2 GB, por lo que NO pueden
#  empaquetarse ahi. Con este set el servicio arranca con
#  OCR_ENGINE=heuristic (preprocesado con OpenCV + parser de documento).
#
#  Para inferencia real con el modelo preentrenado se despliega el backend
#  en un contenedor (Docker / Hugging Face Spaces / Render) con
#  backend/requirements.txt y se apunta FASTAPI_BASE_URL hacia el.
# ===========================================================================

fastapi>=0.115
pydantic>=2.9
python-multipart>=0.0.12
python-jose[cryptography]>=3.3
python-dotenv>=1.0
Pillow>=10.4
numpy>=1.26
opencv-python-headless>=4.10
