# ===========================================================================
#  Backend FastAPI - dependencias para Serverless Functions (Vercel)
#
#  Vercel incluye RapidOCR + ONNX Runtime en el bundle (ver api/requirements.txt
#  y maxLambdaSize 500mb en vercel.json). Este archivo documenta el set minimo
#  equivalente cuando se empaqueta la API de forma independiente.
#
#  PyTorch + EasyOCR (~2 GB) no caben en Serverless. El contenedor Docker
#  (backend/requirements-rapidocr.txt) sigue siendo la via recomendada para
#  desacoplar el OCR del limite de la funcion.
# ===========================================================================

fastapi>=0.115
pydantic>=2.9
python-multipart>=0.0.12
python-dotenv>=1.0
Pillow>=10.4
numpy>=1.26

# OpenCV se fija a la rama 4.x: la 5 elimino CascadeClassifier (deteccion de
# rostro). headless = sin libGL (obligatorio en Serverless).
opencv-python-headless>=4.10,<5

rapidocr>=1.2
onnxruntime>=1.18
